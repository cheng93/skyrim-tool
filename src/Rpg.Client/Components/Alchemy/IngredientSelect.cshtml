<select @onchange((item) => Change((string)item))>
    <option value=""></option>
    @foreach (var ingredient in Ingredients)
    {
        <option value=@ingredient.Id label=@ingredient.Name />
    }
</select>

@functions
{
    public Ingredient[] Ingredients { get; set; }

    public Action<string> Change { get; set; }
}